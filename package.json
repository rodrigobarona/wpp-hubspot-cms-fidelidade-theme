{
  "name": "@fidelidade/cms-theme",
  "version": "1.0.0",
  "description": "Fidelidade's unified HubSpot CMS theme for websites, landing pages, microsites, emails, and more",
  "scripts": {
    "dev": "cd src/fidelidade-theme && bun run start",
    "build": "vite build",
    "upload": "hs project upload",
    "build-upload": "vite build && hs project upload",
    "yarn-start": "cd src/fidelidade-theme && yarn start",
    "npm-start": "cd src/fidelidade-theme && npm run start",
    "test": "vitest",
    "test-verbose": "vitest --reporter verbose",
    "npm-install:all": "npm install && npm install --prefix src/fidelidade-theme",
    "yarn-install:all": "yarn && cd src/fidelidade-theme && yarn install"
  },
  "author": "WPP Portugal",
  "license": "Apache-2.0",
  "devDependencies": {
    "@spingroup/postcss-hubl": "^1.1.5",
    "@vitejs/plugin-react": "^6.0.1",
    "concurrently": "^8.2.1",
    "husky": "7.0.4",
    "jsdom": "^29.0.0",
    "postcss": "^8.4.28",
    "postcss-discard-duplicates": "^6.0.0",
    "postcss-import": "^15.1.0",
    "postcss-nesting": "^12.0.1",
    "postcss-preset-env": "^9.1.1",
    "vite": "^4.4.9",
    "vitest": "^1.6.0"
  },
  "browserslist": [
    "defaults"
  ]
}
